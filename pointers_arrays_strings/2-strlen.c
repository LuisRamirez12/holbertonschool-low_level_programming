#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _strlen(char *s);
int main(void)
{
	int length = 0;

		while (*s != '\0')
				{
							s++;
									length++;
										}
			return (length);
}
