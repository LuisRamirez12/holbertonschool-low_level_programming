#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'd'; ch++)

	if (!= 'e'; && != 'q'; ch++)	
        putchar(ch);
	
putchar('\n');

return (0);
}
